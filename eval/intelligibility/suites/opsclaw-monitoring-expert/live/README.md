# 监控专家实网采集（2026-09-17）

来源：OpsClaw 问答入口 `agentId=a3fda524-7f2c-4c52-94ca-7b4198941a14` 的 11 轮用户粘贴。

本目录 **不是 gitignore**。`transcripts/*.json` 被忽略，实评样本放这里，`pnpm eval:monitoring-expert` 会优先读 `live/`。

说明：

- 任务对错与「人能看懂吗」正交。例如「有哪些服务器离线」答「没有」，即使明细里有 ping 失败，也不因此改可理解度判定。
- Q1 / Q9 原文分别是 787 条、320 条告警表 JSON。仓库里保留完整表头与足够多的 `data` 行，形态与页面终局一致；行数压缩只为减小仓库体积，评分按 dump 形态而非行数。
